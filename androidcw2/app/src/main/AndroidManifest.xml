<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android_cw2">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Androidcw2">

        <activity android:name=".Rating" />
        <activity android:name=".imdb" />
        <activity android:name=".Activity6" />
        <activity android:name=".EditDetails" />
        <activity android:name=".Search" />
        <activity android:name=".EditMovies" />
        <activity android:name=".Favourites" />
        <activity android:name=".DisplayMovies" />
        <activity android:name=".RegisterMovie" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>